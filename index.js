var typed = new Typed('#element', {
    strings: ['Web Developer', 'Backend', 'Database Admin '],
    typeSpeed: 50,
});
document.getElementById("contact-form").addEventListener("submit", function (event) {
    event.preventDefault();

});